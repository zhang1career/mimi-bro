{
  "worker": {
    "id": "test-parallel-deps",
    "objective": "Output a message: {{requirement}}",
    "instructions": [
      "The requirement is provided in the objective above. Do NOT search for requirement files in the repository.",
      "Execute the task as specified.",
      "Mark agent output as generated."
    ],
    "entrypoint": "."
  },
  "plans": [
    { "id": "agent-first", "mode": "agent" },
    { "id": "agent-parallel-a", "mode": "agent", "objective": "say 'World'", "deps": ["agent-first"] },
    { "id": "agent-parallel-b", "mode": "agent", "objective": "say 'everyone'", "deps": ["agent-first"] },
    { "id": "agent-final", "mode": "agent", "objective": "say '!'", "deps": ["agent-parallel-a", "agent-parallel-b"] }
  ],
  "params": {
    "requirement": "Describe the parallel test with dependencies"
  }
}
